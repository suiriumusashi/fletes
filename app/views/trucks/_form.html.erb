<main id="cont-forms-controllers">
  <section class="cont">
    <%= form_for(@truck) do |f| %>
      <% if @truck.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@truck.errors.count, "error") %> prohibited this truck from being saved:</h2>

          <ul>
          <% @truck.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="cont_fields">
        <%= f.label :name, "Tipo de camion" %><br>
        <%= f.text_field :name %>
      </div>
      <%= f.submit "Guardar", :class => "submit-log-in-user"%>
    <% end %>
