<main id="cont-info-controllers">
  <section class="cont">
    <p>
      <strong>Name:</strong>
      <%= @contact.name %>
    </p>

    <p>
      <strong>Email:</strong>
      <%= @contact.email %>
    </p>

    <p>
      <strong>Phone:</strong>
      <%= @contact.phone %>
    </p>

    <p>
      <strong>Web:</strong>
      <%= @contact.web %>
    </p>

    <p>
      <strong>Subject:</strong>
      <%= @contact.subject %>
    </p>

    <p>
      <strong>Message:</strong>
      <%= @contact.message %>
    </p>
  </section>
</main>

